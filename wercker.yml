box: debian
build:
  steps:
    - install-packages:
        packages: git
    - script:
        name: download theme
        code: |
            $(git clone https://github.com/sethmacleod/dimension.git ./themes/dimension)
    - arjen/hugo-build:
        version: "HEAD"
        theme: dimension
        config: config.toml
        flags: --buildDrafts=true
    